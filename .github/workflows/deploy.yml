      - name: Deploy
        run: |
          forge script script/Deploy.s.sol:Deploy \
          --rpc-url $RPC_URL \
          --private-key $PRIVATE_KEY \
          --broadcast
        working-directory: ./আপনার_ফোল্ডারের_নাম  # যদি প্রোজেক্ট সাব-ফোল্ডারে থাকে
